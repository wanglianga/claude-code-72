{
  "name": "neighbor-kitchen",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "security-check": "esbuild scripts/security-check.ts --bundle --platform=node --format=esm --outfile=node_modules/.cache/security-check.mjs --log-level=warning && node node_modules/.cache/security-check.mjs"
  },
  "dependencies": {
    "pinia": "^2.3.1",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "~5.7.3",
    "vite": "^6.2.0",
    "vue-tsc": "^2.2.0"
  }
}
